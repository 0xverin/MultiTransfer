export interface Token {
    address: string;
    name: string;
    symbol: string;
    decimals: number;
    logoURI?: string;
    chainId: number | undefined;
}
export interface Address {
    // 97?: string;
    // 56: string;
    4: string;
}
